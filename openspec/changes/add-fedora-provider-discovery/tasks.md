## 1. Provider Discovery

- [x] 1.1 Add failing Fedora provider discovery tests with hermetic HTTP fixtures
- [x] 1.2 Implement Fedora discovery family, release parsing, and netboot probes

## 2. Runtime Wiring

- [x] 2.1 Add failing provider config and default registry tests for Fedora discovery settings
- [x] 2.2 Wire Fedora discovery URL and timeout through provider config and default registration

## 3. CLI And Docs

- [x] 3.1 Add failing CLI test for `discover-targets --discovery-family=fedora`
- [x] 3.2 Document Fedora discovery usage and configuration

## 4. Verification

- [x] 4.1 Run Go, lint, build, and OpenSpec validation gates
