## 1. Catalog Contract

- [x] 1.1 Add failing provider registry tests for typed catalog metadata and invalid provider targets.
- [x] 1.2 Add the typed catalog metadata model to `provider.Target`.
- [x] 1.3 Validate provider target IDs, provider IDs, names, and catalog metadata in `Registry.Targets`.

## 2. Provider And UI Wiring

- [x] 2.1 Move Debian and Ubuntu provider target metadata into the catalog field.
- [x] 2.2 Update text and rich UI rendering to read catalog metadata from the typed catalog field.
- [x] 2.3 Update app, provider, UI, fixture, and command tests for the catalog contract.

## 3. Documentation And Validation

- [x] 3.1 Document the static catalog mode and deferred dynamic modes.
- [x] 3.2 Run Go tests, linting, build, and OpenSpec validation.
- [x] 3.3 Archive the completed OpenSpec change after implementation.
